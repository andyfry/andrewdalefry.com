{% extends "base.njk" %}

{% block main %}
    <h1>{{title}}</h1>
    <h2>
        <span class="label">Published:</span><span class="date">{{page.date | date}}</span>
    </h2>
    {{content | safe}}
{% endblock %}

{% block sidebar %}
    <nav class="pages">
        <h2>Videos</h2>
        <ul>
            {% for series in collections.gamingSeries %}
                <li>
                    <a {%if series.url === page.url%} class="active" {% endif%} href="{{series.url}}">{{series.data.title}}</a>
                </li>
            {% endfor %}
        </ul>
    </nav>
{% endblock %}